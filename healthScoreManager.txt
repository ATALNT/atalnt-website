map healthScoreManager()
{
	apiKey = "Njg1ZDRmNDYtYWM1Mi00YzYyLWEzMTktNDRhNzFlNjA4YTE2OmtCWUxEelR0cFdjTg==";
	minScore = 97;
	pausedCount = 0;
	resumedCount = 0;
	skippedCount = 0;
	resultMap = Map();
	headers = Map();
	headers.put("Authorization","Bearer " + apiKey);
	headers.put("Content-Type","application/json");
	baseUrl = "https://api.instantly.ai/api/v2/accounts?limit=100";
	allAccounts = List();
	continueFlag = true;
	startAfter = "";
	resp = invokeurl
	[
		url :baseUrl
		type :GET
		headers:headers
	];
	items = resp.get("items");
	for each  item in items
	{
		allAccounts.add(item);
	}
	if(items.size() == 100)
	{
		startAfter = items.get(99).get("email");
	}
	else
	{
		continueFlag = false;
	}
	if(continueFlag)
	{
		resp = invokeurl
		[
			url :baseUrl + "&starting_after=" + startAfter
			type :GET
			headers:headers
		];
		items = resp.get("items");
		for each  item in items
		{
			allAccounts.add(item);
		}
		if(items.size() == 100)
		{
			startAfter = items.get(99).get("email");
		}
		else
		{
			continueFlag = false;
		}
	}
	if(continueFlag)
	{
		resp = invokeurl
		[
			url :baseUrl + "&starting_after=" + startAfter
			type :GET
			headers:headers
		];
		items = resp.get("items");
		for each  item in items
		{
			allAccounts.add(item);
		}
		if(items.size() == 100)
		{
			startAfter = items.get(99).get("email");
		}
		else
		{
			continueFlag = false;
		}
	}
	if(continueFlag)
	{
		resp = invokeurl
		[
			url :baseUrl + "&starting_after=" + startAfter
			type :GET
			headers:headers
		];
		items = resp.get("items");
		for each  item in items
		{
			allAccounts.add(item);
		}
		if(items.size() == 100)
		{
			startAfter = items.get(99).get("email");
		}
		else
		{
			continueFlag = false;
		}
	}
	if(continueFlag)
	{
		resp = invokeurl
		[
			url :baseUrl + "&starting_after=" + startAfter
			type :GET
			headers:headers
		];
		items = resp.get("items");
		for each  item in items
		{
			allAccounts.add(item);
		}
		if(items.size() == 100)
		{
			startAfter = items.get(99).get("email");
		}
		else
		{
			continueFlag = false;
		}
	}
	if(continueFlag)
	{
		resp = invokeurl
		[
			url :baseUrl + "&starting_after=" + startAfter
			type :GET
			headers:headers
		];
		items = resp.get("items");
		for each  item in items
		{
			allAccounts.add(item);
		}
		if(items.size() == 100)
		{
			startAfter = items.get(99).get("email");
		}
		else
		{
			continueFlag = false;
		}
	}
	if(continueFlag)
	{
		resp = invokeurl
		[
			url :baseUrl + "&starting_after=" + startAfter
			type :GET
			headers:headers
		];
		items = resp.get("items");
		for each  item in items
		{
			allAccounts.add(item);
		}
		if(items.size() == 100)
		{
			startAfter = items.get(99).get("email");
		}
		else
		{
			continueFlag = false;
		}
	}
	if(continueFlag)
	{
		resp = invokeurl
		[
			url :baseUrl + "&starting_after=" + startAfter
			type :GET
			headers:headers
		];
		items = resp.get("items");
		for each  item in items
		{
			allAccounts.add(item);
		}
	}
	for each  account in allAccounts
	{
		email = account.get("email");
		healthScore = account.get("stat_warmup_score");
		status = account.get("status");
		if(healthScore != null)
		{
			if(healthScore >= minScore && status != 1)
			{
				invokeurl
				[
					url :"https://api.instantly.ai/api/v2/accounts/" + email + "/resume"
					type :POST
					headers:headers
				];
				resumedCount = resumedCount + 1;
			}
			else if(healthScore < minScore && status == 1)
			{
				invokeurl
				[
					url :"https://api.instantly.ai/api/v2/accounts/" + email + "/pause"
					type :POST
					headers:headers
				];
				pausedCount = pausedCount + 1;
			}
			else
			{
				skippedCount = skippedCount + 1;
			}
		}
		else
		{
			skippedCount = skippedCount + 1;
		}
	}
	resultMap.put("turned_on",resumedCount);
	resultMap.put("turned_off",pausedCount);
	resultMap.put("skipped",skippedCount);
	resultMap.put("total",allAccounts.size());
	return resultMap;
}
